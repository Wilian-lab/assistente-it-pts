export function useTrainingAlerts() {
  return {
    data: [],
    isLoading: false,
    isError: false,
    gap: 'Endpoint GET /api/admin/training/alerts ainda não existe oficialmente no backend Java.',
  }
}

